#ifndef VERSION_H
#define VERSION_H

namespace GameConstants {
    // qmake will replace these with the values from the .pro file
    static const char* const GIT_HASH = "$$VERSION_HASH";
    static const char* const BUILD_TIMESTAMP = "$$VERSION_DATE";
    static const char* const FULL_VERSION = "$$VERSION_INT-$$VERSION_HASH";
}

#endif
